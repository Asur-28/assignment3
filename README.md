# Assignment 3: Functions & Modules in Python

## Task 1: Factorial Calculator
- Defines a function `factorial(n)` using a loop.
- Handles edge cases like negative input.
- Returns and prints the factorial of the input number.

## Task 2: Math Module Operations
- Takes a number as input.
- Uses Python's `math` module to compute:
  - Square root
  - Natural logarithm (base e)
  - Sine (in radians)
- Displays results with basic input validation.
